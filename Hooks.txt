훅스 정리
()=>{} / 일반 함수에서는 this를 사용할 수 있지만, 화살표 함수에서는 this로 정의해리면 윈도우를 잡아버린다.
          

usestate() - 상태관리 함수

useEffect() - 메인 파일을 불러왔을때, 이 안에 있는 함수들은 자동 실행된다.

  useEffect(()=>{
    
  },[])     
 
useEffect의 기본 형태이다. 대괄호 안에 내용의 변화를 감지하고 실행시킬 구문을 적으면,
변화 할 때 마다 실행된다. 서버에서 받아온 데이터를 지정된 장소에 뿌려줄때 사용한다.

 
